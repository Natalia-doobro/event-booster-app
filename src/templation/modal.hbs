{{#each this}}
  <div class='modal_content'>
    <div class='img'>
      <img src='{{images.3.url}}' alt='{{name}}' class='img-card_image_small' />
      <img src='{{images.0.url}}' alt='{{name}}' class='img-card_image' />
    </div>
    <h2 class='modal-title'>INFO</h2>
    <p class='modal-text'>{{info}}</p>
    <h2 class='modal-title'>WHEN</h2>
    <p class='modal-text'>{{dates.start.localDate}}</p>
    <p class='modal-text'>{{dates.start.localTime}} ({{dates.timezone}})</p>
    <h2 class='modal-tittle'>WHERE</h2>
    <p class='modal-text'>{{dates.timezone}}</p>
    <p class='modal-text'>{{_embedded.venues.0.address.line1}}</p>
    <h2 class='modal-title'>WHO</h2>
    <p class='modal-text'>{{name}}</p>
    <h2 class='modal-title'>PRICES</h2>
    <span class='material-icons'>qr_code</span>
    <p class='modal-text'>
      {{priceRanges.0.min}}-{{priceRanges.0.max}} {{priceRanges.0.currency}}</p>
    <button type='button' class='modal_button'>BUY TICKETS</button>
    <span class='material-icons'>qr_code</span>
    <p class='modal-text'>
      {{priceRanges.0.min}}-{{priceRanges.0.max}} {{priceRanges.0.currency}}</p>
    <button type='button' class='modal_button'>BUY TICKETS</button>
  </div>
{{/each}}